{
  "name": "todo-fullstack",
  "version": "1.0.0",
  "scripts": {
    "start": "docker-compose up --build",
    "stop": "docker-compose down",
    "stop:clean": "docker-compose down -v"
  }
}
